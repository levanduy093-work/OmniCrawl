{
  "name": "omnicrawl",
  "version": "0.0.1",
  "description": "Local-first web dashboard and Chrome Browser Agent for Shopee and TikTok crawling.",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @omnicrawl/database build && pnpm --filter @omnicrawl/sdk build && pnpm --filter @omnicrawl/api build && pnpm --filter @omnicrawl/dashboard build",
    "dev": "pnpm --filter @omnicrawl/database build && pnpm --filter @omnicrawl/sdk build && pnpm --parallel --filter @omnicrawl/api --filter @omnicrawl/dashboard dev",
    "lint": "pnpm --filter @omnicrawl/dashboard lint",
    "db:push": "pnpm --filter @omnicrawl/database db:push",
    "db:seed": "pnpm --filter @omnicrawl/database db:seed"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
